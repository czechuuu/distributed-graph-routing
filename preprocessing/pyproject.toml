[project]
name = "preprocessing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dgr-shared",
    "apache-beam[gcp]>=2.70.0",
    "build>=1.4.0",
    "functions-framework>=3.10.0",
    "google-cloud-bigquery>=3.40.0",
    "google-cloud-bigtable>=2.35.0",
    "grpcio-tools>=1.65.5",
    "igraph>=0.11.0",
    "pip",
    "crcmod",
    "s2sphere>=0.2.5",
]

[tool.uv.sources]
dgr-shared = { workspace = true }
