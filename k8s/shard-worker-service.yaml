apiVersion: v1
kind: Service
metadata:
  name: shard-worker
spec:
  clusterIP: None
  selector:
    app: shard-worker
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
