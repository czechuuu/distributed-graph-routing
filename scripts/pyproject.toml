[project]
name = "scripts"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
    "google-cloud-storage>=2.18.0",
    "grpcio-tools",
    "igraph",
    "osmium>=4.0.0",
    "pyarrow>=17.0.0",
    "protobuf",
]

[tool.uv]
package = false
